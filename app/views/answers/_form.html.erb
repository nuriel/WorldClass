<%= form_for([@answer.question.classroom, @answer.question, @answer]) do |f| %>
  <% if @answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% @answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :rating %><br />
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :solved %><br />
    <%= f.check_box :solved %>
  </div>
  <div class="field">
    <%= f.label :approved_by_teacher %><br />
    <%= f.check_box :approved_by_teacher %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
